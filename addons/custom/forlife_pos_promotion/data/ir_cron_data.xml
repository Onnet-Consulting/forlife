<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_auto_close_promotion_campaign" model="ir.cron">
            <field name="name">Promotion: Auto Close Promotion Campaign</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="model_id" ref="model_promotion_campaign"/>
            <field name="nextcall" eval="DateTime.now().strftime('%Y-%m-%d 05:00:00')"/>
            <field name="state">code</field>
            <field name="code">model.auto_close_promotion_campaign()</field>
        </record>

    </data>
</odoo>