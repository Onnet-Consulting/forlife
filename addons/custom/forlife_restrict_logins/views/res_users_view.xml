<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="res_users_restrict_logins_tree" model="ir.ui.view">
            <field name="name">res.users.tree.restrict.logins</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='login_date']" position="replace">
                    <field name="sid" string="Current Session Expiry" readonly="1"/>
                    <field name="exp_date" string="Current Session Expiry" readonly="1"/>
                    <field name="login_date" string="Last Login" readonly="1"/>
                    <!--                    <field name="last_update" string="Last Connection" readonly="1"/>-->
                    <field name="restrict_login_only_1_browser" string="Restrict login only for 1 device" readonly="0"/>
                </xpath>
            </field>
        </record>
        <!-- Inherit Form View to Modify it -->
        <record id="res_users_restrict_logins_form" model="ir.ui.view">
            <field name="name">res.users.form.restrict.logins</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">

                <xpath expr="//div[hasclass('oe_title')]/group" position="inside">
                    <!-- Add your fields or attributes here -->
                    <field name="restrict_login_only_1_browser"/>
                    <field name="session_life_time"/>
                </xpath>

            </field>
        </record>
    </data>
</odoo>