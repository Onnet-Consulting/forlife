<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="product_inventory_by_period_company_rule" model="ir.rule">
            <field name="name">Cuối kỳ theo công ty</field>
            <field name="model_id" ref="model_product_inventory_by_period"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="calculate_final_value_company_rule" model="ir.rule">
            <field name="name">Cuối kỳ theo công ty</field>
            <field name="model_id" ref="model_calculate_final_value"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

        <record id="accounting_difference_company_rule" model="ir.rule">
            <field name="name">Cuối kỳ theo công ty</field>
            <field name="model_id" ref="model_accounting_difference"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

    </data>
</odoo>