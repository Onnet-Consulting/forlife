<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_check_finish_points_promotion" model="ir.cron">
            <field name="name">Check Finish Points Promotion</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field ref="model_points_promotion" name="model_id"/>
            <field name="nextcall" eval="DateTime.now().strftime('%Y-%m-%d 17:00:00')"/>
            <field name="state">code</field>
            <field name="code">model.check_finish_points_promotion()</field>
        </record>

    </data>
</odoo>