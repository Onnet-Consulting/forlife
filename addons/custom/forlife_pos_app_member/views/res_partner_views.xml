<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//span[@name='address_name']" position="before">
                <field name="ref" string="Mã"/>
            </xpath>
            <field name="vat" position="before">
                <field name="group_id" required="1"/>
                <field name="retail_type_ids" widget="many2many_tags" attrs="{'invisible': [('show_customer_type', '=', False)]}"/>
                <field name="show_customer_type" invisible="1"/>
            </field>
            <field name="company_registry" position="before">
                <field name="job_ids" widget="many2many_tags"/>
            </field>

        </field>
    </record>
        <record id="res_partner_form_email_inherit" model="ir.ui.view">
        <field name="name">res.partner.email</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="mail.res_partner_view_form_inherit_mail"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='email']/.." position="after">
                <field name="gender"/>
                <field name="birthday"/>
            </xpath>
            <field name="email" position="before">

            </field>
        </field>
    </record>


</odoo>