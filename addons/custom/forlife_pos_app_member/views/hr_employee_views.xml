<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="hr_employee_form_inherit" model="ir.ui.view">
        <field name="name">name</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_create_related_user" type="object" string="Create user" class="oe_highlight"
                        attrs="{'invisible': ['|', ('user_id', '!=', False), ('id', '=', False)]}"
                        groups="base.group_system"/>
            </xpath>
            <field name="coach_id" position="after">
                <field name="code"/>
            </field>
            <xpath expr="//group[@name='active_group']/field[@name='user_id']" position="attributes">
                <attribute name="readonly">1</attribute>
            </xpath>
        </field>
    </record>

    <record id="action_create_user_from_list" model="ir.actions.server">
        <field name="name">Create users</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="binding_model_id" ref="hr.model_hr_employee"/>
        <field name="binding_view_types">list</field>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
        <field name="state">code</field>
        <field name="code">
            if records:
            records.action_create_related_user()
        </field>
    </record>
</odoo>