<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="action_sync_account_move_bravo" model="ir.actions.server">
            <field name="name">Đẩy lên Bravo</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
            if records:
                res = records.filtered(lambda f: not f.is_bravo_pushed and f.state == 'posted').action_sync_account_move()
            </field>
        </record>
</odoo>